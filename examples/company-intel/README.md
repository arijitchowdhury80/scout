# Company Intelligence Example

```bash
scout map https://example.com --pages 200
scout scrape https://example.com/about --js
scout crawl https://example.com/careers --depth 2 --pages 30 --pattern careers
```

Use Scout when a page needs JavaScript rendering, sitemap discovery, PDF text
extraction, or consistent markdown output.
